# 1. Project-Specific Secrets (Crucial for this project!)
# This prevents your Wi-Fi credentials and API keys from being uploaded.
config.h

# 2. Operating System Files
# These are generated by Windows or macOS and are useless to Git.
.DS_Store
Thumbs.db

# 3. Build & IDE Artifacts
# These files are generated when you compile your C++ code.
# They are large, temporary, and should never be committed.
build/
.build/
.vscode/
